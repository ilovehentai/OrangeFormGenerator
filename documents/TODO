- Modo AjaxSubmit no FormGenerator
- Se alterarmos o template a cache nao o vai permitir porque ela so valida o config
- Internacionalizar (falta apenas os default values)
- dizer nas select qual a selected
- aliementar as options, radio, checkbox de algum datasource
- Default empty template
- Add options as collection and options group
- Bug on multi checkbox, unselect recovery when having default pre selected.